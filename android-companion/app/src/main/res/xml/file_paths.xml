<?xml version="1.0" encoding="utf-8"?>
<!--
  Only the app's own downloads directory is shared, and only so a saved report
  can be handed to a PDF viewer. Nothing else in the app is reachable this way.
-->
<paths>
    <external-files-path name="downloads" path="Download/" />
</paths>
